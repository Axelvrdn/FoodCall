export interface ToggleProps { label: string; checked: boolean; onChange?: (checked: boolean) => void; }
export function Toggle({ label, checked, onChange }: ToggleProps) { return <label className="flex items-center justify-between gap-4 rounded-card bg-surface-warm p-4 text-sm font-semibold"><span>{label}</span><button type="button" aria-pressed={checked} onClick={() => onChange?.(!checked)} className={`h-7 w-12 rounded-full p-1 transition-colors transition-transform duration-150 ease-out active:scale-[0.97] ${checked ? 'bg-primary' : 'bg-soft'}`}><span className={`block h-5 w-5 rounded-full bg-surface transition-colors duration-150 ${checked ? 'translate-x-5' : ''}`} /></button></label>; }
