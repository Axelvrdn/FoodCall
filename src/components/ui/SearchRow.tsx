export interface SearchRowProps { onSubmit?: (query: string) => void; }
export function SearchRow({ onSubmit }: SearchRowProps) { return <form onSubmit={(event) => { event.preventDefault(); const form = new FormData(event.currentTarget); onSubmit?.(String(form.get('query') ?? '')); }} className="grid gap-3 rounded-card bg-surface p-4 shadow-soft md:grid-cols-[1fr_180px_auto]"><input name="query" placeholder="Adresse ou quartier" className="rounded-full border border-border px-4 py-3 outline-primary" /><select className="rounded-full border border-border px-4 py-3 outline-primary" defaultValue=""><option value="">Cuisine</option><option>Japonais</option><option>Italien</option></select><button className="rounded-full bg-primary px-6 py-3 font-semibold text-white transition-transform duration-150 ease-out active:scale-[0.97]">Trouver</button></form>; }
